export type metadataType = {
  albumArtist: string;
  albumTitle: string;
  albumUrl: string;
  songTitle: string;
  songFormat: string;
  songDepth: number;
  songQuality: string;
  songRate: number;
  songBitrate: number;
  artist: string;
  album: string;
  biography: string;
  streamSource: string;
};

export type playerType = {
  status: string;
};

export type detailsType = {
  biography: string;
};

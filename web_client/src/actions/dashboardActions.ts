import {
    PROXY_URL,
    PROXY_PORT
} from './../defaults/constants';

import { HTTP } from './fetchActions'

export async function registerExpectation() {
   // var url = PROXY_URL + "/" + PROXY_PORT;
   // let result = await HTTP(url, null, undefined, "POST");
   // return result;
}